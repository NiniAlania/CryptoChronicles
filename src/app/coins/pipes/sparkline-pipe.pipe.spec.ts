import { SparklinePipePipe } from './sparkline-pipe.pipe';

describe('SparklinePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SparklinePipePipe();
    expect(pipe).toBeTruthy();
  });
});
